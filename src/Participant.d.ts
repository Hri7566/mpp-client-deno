type Participant = {
    _id: string;    // user id
    name: string;   // user name
    color: string;  // user color
    id: string;    // participant id
    x?: number;
    y?: number;
}

export {
    Participant
}
